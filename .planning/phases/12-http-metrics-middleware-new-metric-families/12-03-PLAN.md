---
phase: 12-http-metrics-middleware-new-metric-families
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/aws/discovery.rs
  - src/state/app_state.rs
autonomous: true
requirements: [MET-10, MET-11, MET-12]
must_haves:
  truths:
    - "Every AWS SDK .send().await site in discovery.rs is preceded by an aws_api_calls_total{operation} increment"
    - "AppState::replace_cache_and_record_metrics computes per-cluster target counts and target-address churn, then atomically replaces the cache"
    - "Per-cluster counts group by __meta_ecs_cluster_name (unknown fallback); churn added = new-minus-old, removed = old-minus-new address-set diff"
  artifacts:
    - path: "src/aws/discovery.rs"
      provides: "metrics field on DiscoveryService + per-operation instrumentation"
      contains: "aws_api_calls_total"
    - path: "src/state/app_state.rs"
      provides: "replace_cache_and_record_metrics method + pure churn/per-cluster helpers"
      contains: "pub async fn replace_cache_and_record_metrics"
  key_links:
    - from: "src/state/app_state.rs replace_cache_and_record_metrics"
      to: "self.replace_cache_and_routing"
      via: "atomic swap after reading old addresses"
      pattern: "replace_cache_and_routing"
    - from: "src/aws/discovery.rs DiscoveryService::new"
      to: "src/state/app_state.rs AppState::new"
      via: "metrics arg passed through"
      pattern: "DiscoveryService::new"
---

<objective>
Instrument the discovery service layer with the operational metrics that do not require touching main.rs: per-AWS-operation call counting (MET-12) inside `DiscoveryService`, and a new `AppState::replace_cache_and_record_metrics` method that records per-cluster target counts (MET-10) and target churn (MET-11) around the existing atomic cache swap. This plan defines the reusable method; plan 12-04 wires it into all three refresh paths.

Purpose: Centralizing the per-cluster + churn logic in one `AppState` method (with pure, unit-tested helpers) means the background loop, manual refresh, and follower sync all share one correct implementation. Adding the `metrics` handle to `DiscoveryService` lets every AWS `.send()` site be counted.
Output: `DiscoveryService` gains a `metrics` field and a call-count increment before every AWS `.send().await`; `AppState` gains `replace_cache_and_record_metrics` plus two pure helper fns with unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-http-metrics-middleware-new-metric-families/12-RESEARCH.md
@.planning/codebase/TESTING.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from source-drift verification):

- Field `metrics: std::sync::Arc<crate::metrics::MetricsState>` on `DiscoveryService` (`src/aws/discovery.rs`)
- Changed signature: `DiscoveryService::new(..., metrics: Arc<crate::metrics::MetricsState>)` gains a trailing `metrics` parameter
- Method `AppState::replace_cache_and_record_metrics(&self, targets_aws: Vec<Target>)` (`src/state/app_state.rs`)
- Pure helper fns in `src/state/app_state.rs`: `per_cluster_target_counts(targets: &[Target]) -> HashMap<String, usize>` and `target_address_churn(old: &HashSet<String>, new: &HashSet<String>) -> (usize, usize)`
- Consumes MetricsState fields `aws_api_calls_total`, `discovery_targets_per_cluster`, `discovery_target_churn_total` (created in plan 12-01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add metrics field to DiscoveryService and count every AWS .send() site (MET-12)</name>
  <files>src/aws/discovery.rs</files>
  <read_first>
    - src/aws/discovery.rs (struct at lines 102-108, `new()` at 122-146, and every `.send()` call site — confirmed at lines 131, 196, 220, 239, 478, 529, 562, 587, 612, 673, 688 = 11 sites)
    - src/state/app_state.rs (AppState::new at 84-114 — line 94 constructs DiscoveryService; you add the metrics arg there in Task 2, but confirm the `metrics` param name now)
    - .planning/phases/12-http-metrics-middleware-new-metric-families/12-RESEARCH.md (Pattern 3 — operation label names per call site; Pitfall 2 — paginated loops counting per round-trip is CORRECT)
  </read_first>
  <action>
    Add `metrics: std::sync::Arc<crate::metrics::MetricsState>` as the last field of the `DiscoveryService` struct (the struct already derives `Clone`; `Arc` is `Clone`, so no change to derives). Add a trailing `metrics: std::sync::Arc<crate::metrics::MetricsState>` parameter to `DiscoveryService::new` and store it in the returned `Self`.

    Immediately before EACH of the 11 `.send().await` call sites, insert `self.metrics.aws_api_calls_total.with_label_values(&["<operation>"]).inc();` (for the site inside `new()`, use `metrics.aws_api_calls_total...` since `self` does not yet exist). The operation label for each site MUST be the exact AWS operation name:
    - line ~131 (`get_caller_identity().send()` in `new()`): `"get_caller_identity"`
    - line ~196 (`describe_clusters().send()`): `"describe_clusters"`
    - line ~220 (`describe_services().send()`): `"describe_services"`
    - line ~239 (`describe_tasks().send()`): `"describe_tasks"`
    - line ~478 (`describe_task_definition().send()`): `"describe_task_definition"`
    - line ~529 (`list_services().send()`): `"list_services"`
    - line ~562 (`list_tasks().send()`): `"list_tasks"`
    - line ~587 (`describe_container_instances().send()` in populate cache): `"describe_container_instances"`
    - line ~612 (`describe_instances().send()` in populate cache): `"describe_instances"`
    - line ~673 (`describe_container_instances().send()` in resolve_ec2_instance): `"describe_container_instances"`
    - line ~688 (`describe_instances().send()` in resolve_ec2_instance): `"describe_instances"`
    Counting once per network round-trip inside the pagination `loop` blocks (`list_all_services`, `list_service_tasks`) is intentional and correct — do not hoist the increment out of the loop. Do not add any test that mocks AWS; MET-12 behavior is covered structurally (parity between `.send()` sites and inc calls) plus the family-existence test in plan 12-01.
  </action>
  <verify>
    <automated>cd /Users/piotrek/git/ecs-sd && cargo build 2>&1 | tail -6 && echo "sends=$(rg -c '\.send\(\)' src/aws/discovery.rs) incs=$(rg -c 'aws_api_calls_total\.with_label_values' src/aws/discovery.rs)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/aws/discovery.rs` struct `DiscoveryService` contains a `metrics: std::sync::Arc<crate::metrics::MetricsState>` field
    - `DiscoveryService::new` signature contains a `metrics:` parameter and the returned `Self` includes `metrics`
    - `rg -c '\.send\(\)' src/aws/discovery.rs` equals `rg -c 'aws_api_calls_total\.with_label_values' src/aws/discovery.rs` (both 11 — one inc per send site)
    - Both `describe_container_instances` sites and both `describe_instances` sites are instrumented (each operation name appears at least twice)
    - `cargo build` exits 0 (note: it will still fail to fully link only if Task 2's app_state call-site update is not yet applied — run Task 2 before final `cargo build` of the plan)
  </acceptance_criteria>
  <done>Every AWS `.send().await` in discovery.rs is preceded by a matching `aws_api_calls_total{operation}` increment, and `DiscoveryService` carries the metrics handle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add replace_cache_and_record_metrics + pure helpers (MET-10, MET-11) and pass metrics into DiscoveryService::new</name>
  <files>src/aws/discovery.rs, src/state/app_state.rs</files>
  <read_first>
    - src/state/app_state.rs (AppState struct, `new()` at 84-114 line 94 DiscoveryService construction, existing `replace_cache_and_routing` at 116-125, and the existing `#[cfg(test)] mod tests` at 128-189 for the test style)
    - src/models/target.rs (Target: `targets: Vec<String>`, `labels: HashMap<String, String>`)
    - .planning/phases/12-http-metrics-middleware-new-metric-families/12-RESEARCH.md (Pattern 2 — full method logic; Pitfall 3 — scope the read-lock guard in a block BEFORE calling replace_cache_and_routing to avoid RwLock deadlock)
  </read_first>
  <behavior>
    - `per_cluster_target_counts`: two targets both labeled cluster `prod` with 1 address each → `{"prod": 2}`; a target missing `__meta_ecs_cluster_name` → counted under `"unknown"`.
    - `target_address_churn`: old `{a,b}`, new `{b,c}` → `(added=1, removed=1)`; old `{}`, new `{a,b}` → `(added=2, removed=0)` (initial population); old `{a,b}`, new `{a,b}` → `(0,0)`.
  </behavior>
  <action>
    Add two pure free functions to `src/state/app_state.rs` (module-level, `pub(crate)` so tests and the method can use them), following the existing free-function style used by `build_snapshot`:
    - `per_cluster_target_counts(targets: &[Target]) -> HashMap<String, usize>`: for each target, read label key `__meta_ecs_cluster_name` (clone), default to `"unknown"`, and add `target.targets.len()` to that cluster's running total.
    - `target_address_churn(old: &HashSet<String>, new: &HashSet<String>) -> (usize, usize)`: return `(new.difference(old).count(), old.difference(new).count())` as `(added, removed)`.
    Add `use std::collections::HashSet;` to the imports (HashMap is already imported).

    Add method `pub async fn replace_cache_and_record_metrics(&self, targets_aws: Vec<Target>)` on `impl AppState`:
    1. Read the old address set inside a scoped block so the read-lock guard is dropped before the atomic swap (Pitfall 3): acquire `self.snapshot.read().await`, take the `MetadataLevel::Aws` cache entry, `flat_map` each target's `targets` into a `HashSet<String>`, default empty; end the block so the guard drops.
    2. Compute `per_cluster_target_counts(&targets_aws)` and, for each `(cluster, count)`, call `self.metrics.discovery_targets_per_cluster.with_label_values(&[cluster]).set(count as f64)` (MET-10).
    3. Build the new address `HashSet<String>` from `&targets_aws` (flat_map of `t.targets`).
    4. Call `self.replace_cache_and_routing(targets_aws).await` (moves the vec; this is the existing atomic swap).
    5. Compute `(added, removed) = target_address_churn(&old, &new)`; if `added > 0` call `discovery_target_churn_total.with_label_values(&["added"]).inc_by(added as f64)`; if `removed > 0` do the same with `["removed"]` (MET-11).
    Do NOT set `startup_duration_seconds` here — MET-14 belongs to the initial-discovery branch in main.rs (plan 12-04); setting it here would update it on every refresh.

    Update `AppState::new` line ~94: pass `std::sync::Arc::clone(&metrics)` as the new trailing arg to `DiscoveryService::new(ecs_client, ec2_client, sts_client, region, std::sync::Arc::clone(&metrics))`. The `metrics` param is still stored in `Self` unchanged.

    Add unit tests to the existing `#[cfg(test)] mod tests` block for `per_cluster_target_counts` and `target_address_churn` covering the cases in the behavior block above (these are pure — no AppState/AWS needed). Do not attempt to unit-test `replace_cache_and_record_metrics` directly (it requires a full AppState with real AWS clients — that path is covered by Phase 15 integration tests).
  </action>
  <verify>
    <automated>cd /Users/piotrek/git/ecs-sd && cargo test app_state 2>&1 | tail -15 && cargo test 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `src/state/app_state.rs` contains `pub async fn replace_cache_and_record_metrics(&self, targets_aws: Vec<Target>)`
    - `src/state/app_state.rs` contains `pub(crate) fn per_cluster_target_counts` and `pub(crate) fn target_address_churn`
    - `replace_cache_and_record_metrics` calls `self.replace_cache_and_routing(` after the old-address read block (source order: read block, then per-cluster set, then swap, then churn inc)
    - `src/state/app_state.rs` line ~94 passes a cloned `metrics` Arc to `DiscoveryService::new`
    - New tests assert `per_cluster_target_counts` returns `{"prod": 2}` for two same-cluster single-address targets and buckets a missing cluster label under `"unknown"`; `target_address_churn` returns `(1,1)` for `{a,b}`→`{b,c}` and `(2,0)` for `{}`→`{a,b}`
    - `cargo test` full suite exits 0
  </acceptance_criteria>
  <done>The reusable metrics-recording method exists with deadlock-safe lock scoping, the pure helpers are unit-tested, DiscoveryService::new receives the metrics handle, and the full suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AWS API → discovery layer | Cluster names in `__meta_ecs_cluster_name` originate from AWS responses (bounded by the operator-configured `ECS_SD_CLUSTERS`); they become the `cluster` gauge label |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Denial of Service | discovery_targets_per_cluster cluster label | mitigate | Cluster label set is bounded by the operator's `ECS_SD_CLUSTERS` config, not by external request input. Missing label falls back to the single fixed value `"unknown"`. |
| T-12-05 | Denial of Service | RwLock deadlock in replace_cache_and_record_metrics | mitigate | Read-lock guard for old-address computation is scoped in a block and dropped before `replace_cache_and_routing` takes the write lock (Pitfall 3). |
| T-12-06 | Denial of Service | aws_api_calls_total operation label | mitigate | Operation labels are hardcoded string literals at each call site (9 distinct values), never derived from external input. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero new crates (RESEARCH `## Package Legitimacy Audit`). |
</threat_model>

<verification>
- `cargo build` compiles the new struct field, `new()` signature, method, and helpers.
- `cargo test app_state` green (pure-helper tests).
- `cargo test` full suite green.
- `.send()`-to-increment parity check in discovery.rs (both 11).
</verification>

<success_criteria>
Every AWS SDK call in discovery.rs is counted by `aws_api_calls_total{operation}`, and `AppState::replace_cache_and_record_metrics` records per-cluster counts and address churn around the existing atomic swap using deadlock-safe lock scoping, with pure helpers proven by unit tests.
</success_criteria>

<output>
Create `.planning/phases/12-http-metrics-middleware-new-metric-families/12-03-SUMMARY.md` when done.
</output>
