---
phase: 12-http-metrics-middleware-new-metric-families
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/metrics/mod.rs
  - src/metrics/tests.rs
autonomous: true
requirements: [MET-08, MET-09, MET-10, MET-11, MET-12, MET-13, MET-14]
must_haves:
  truths:
    - "MetricsState::new() succeeds with 16 total metric families registered in one custom Registry"
    - "Each of the 7 new metric families is gatherable from the registry after first label use"
    - "Label sets are exact: http_requests_total has endpoint/method/status_code; http_request_duration_seconds has endpoint/method (no status_code); churn has change; aws calls has operation; follower syncs has result"
  artifacts:
    - path: "src/metrics/mod.rs"
      provides: "7 new MetricsState fields + registration"
      contains: "pub http_requests_total: CounterVec"
    - path: "src/metrics/tests.rs"
      provides: "Registration + label tests for 7 new families"
      contains: "ecs_sd_http_requests_total"
  key_links:
    - from: "src/metrics/mod.rs MetricsState::new"
      to: "registry.register"
      via: "Box::new(<field>.clone())"
      pattern: "registry\\.register\\(Box::new\\("
---

<objective>
Extend `MetricsState` with the 7 new Prometheus metric families required by Phase 12 (MET-08 through MET-14), registering them in the existing custom `Registry`. This is the foundation wave: every downstream plan (HTTP middleware, discovery instrumentation, refresh wiring) writes into fields defined here.

Purpose: All new metrics must live in the same `MetricsState`/`Registry` that `handlers::metrics::metrics_handler` gathers, so they appear in `GET /metrics` output. Adding a separate registry would silently orphan the metrics.
Output: `src/metrics/mod.rs` with 7 new public fields constructed and registered; `src/metrics/tests.rs` with unit tests proving each family is registerable and gatherable with the correct label set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-http-metrics-middleware-new-metric-families/12-RESEARCH.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from source-drift verification):

MetricsState fields (in `src/metrics/mod.rs`):
- `http_requests_total: CounterVec` — metric `ecs_sd_http_requests_total`, labels `endpoint`, `method`, `status_code`
- `http_request_duration_seconds: HistogramVec` — metric `ecs_sd_http_request_duration_seconds`, labels `endpoint`, `method`
- `discovery_targets_per_cluster: GaugeVec` — metric `ecs_sd_discovery_targets_per_cluster`, labels `cluster`
- `discovery_target_churn_total: CounterVec` — metric `ecs_sd_discovery_target_churn_total`, labels `change`
- `aws_api_calls_total: CounterVec` — metric `ecs_sd_aws_api_calls_total`, labels `operation`
- `cache_follower_syncs_total: CounterVec` — metric `ecs_sd_cache_follower_syncs_total`, labels `result`
- `startup_duration_seconds: Gauge` — metric `ecs_sd_startup_duration_seconds`

New imports: `GaugeVec`, `HistogramVec` (from `prometheus`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 7 new metric fields to MetricsState and register them</name>
  <files>src/metrics/mod.rs</files>
  <read_first>
    - src/metrics/mod.rs (current struct + new() — replicate the existing construct-then-register-then-return-Self ordering exactly)
    - src/metrics/tests.rs (existing gather()-based assertions — Task 2 extends this)
    - .planning/phases/12-http-metrics-middleware-new-metric-families/12-RESEARCH.md (Pattern 4 and Pattern 5 — field names, metric names, label sets, bucket config)
  </read_first>
  <behavior>
    - After MetricsState::new(), a caller can access all 7 new public fields.
    - registry.gather() returns families for `ecs_sd_startup_duration_seconds` immediately (Gauge) and for each Vec family after its first `with_label_values` call.
    - `http_requests_total` accepts exactly 3 label values; `http_request_duration_seconds` accepts exactly 2; `discovery_targets_per_cluster` 1; `discovery_target_churn_total` 1; `aws_api_calls_total` 1; `cache_follower_syncs_total` 1.
  </behavior>
  <action>
    Extend the `use prometheus::{...}` line at the top of `src/metrics/mod.rs` to also import `GaugeVec` and `HistogramVec` (keep the existing `Registry, Counter, Gauge, Histogram, HistogramOpts, CounterVec, Opts`).

    Add 7 new public fields to the `MetricsState` struct after the existing cluster metrics block, grouped under a `// New metric families (MET-08..MET-14)` comment banner matching the existing per-group banner style:
    - `pub http_requests_total: CounterVec` (metric name `ecs_sd_http_requests_total`, help `"Total HTTP requests"`, labels `&["endpoint", "method", "status_code"]`)
    - `pub http_request_duration_seconds: HistogramVec` (metric name `ecs_sd_http_request_duration_seconds`, help `"HTTP request duration in seconds"`, buckets `prometheus::exponential_buckets(0.001, 2.0, 14).unwrap()`, labels `&["endpoint", "method"]` — no `status_code` label)
    - `pub discovery_targets_per_cluster: GaugeVec` (metric name `ecs_sd_discovery_targets_per_cluster`, help `"Targets per ECS cluster"`, labels `&["cluster"]`)
    - `pub discovery_target_churn_total: CounterVec` (metric name `ecs_sd_discovery_target_churn_total`, help `"Target churn since last refresh"`, labels `&["change"]`)
    - `pub aws_api_calls_total: CounterVec` (metric name `ecs_sd_aws_api_calls_total`, help `"AWS SDK API calls"`, labels `&["operation"]`)
    - `pub cache_follower_syncs_total: CounterVec` (metric name `ecs_sd_cache_follower_syncs_total`, help `"Follower cache sync outcomes"`, labels `&["result"]`)
    - `pub startup_duration_seconds: Gauge` (metric name `ecs_sd_startup_duration_seconds`, help `"Seconds from process start to first successful cache population"`)

    Construct each via `CounterVec::new(Opts::new(name, help), &labels)?`, `HistogramVec::new(HistogramOpts::new(name, help).buckets(...), &labels)?`, `GaugeVec::new(Opts::new(name, help), &labels)?`, and `Gauge::new(name, help)?`, following the exact `?`-propagating construction style already used for the 9 existing metrics. Register each with `registry.register(Box::new(<field>.clone()))?` in the same registration block as the existing metrics, and add each field to the returned `Ok(Self { ... })` literal. Do NOT introduce a second Registry or use `prometheus::default_registry()`.
  </action>
  <verify>
    <automated>cd /Users/piotrek/git/ecs-sd && cargo build 2>&1 | tail -5 && rg -c 'registry\.register\(Box::new\(' src/metrics/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - `src/metrics/mod.rs` first `use prometheus::{...}` line contains `GaugeVec` and `HistogramVec`
    - `src/metrics/mod.rs` contains `pub http_requests_total: CounterVec`, `pub http_request_duration_seconds: HistogramVec`, `pub discovery_targets_per_cluster: GaugeVec`, `pub discovery_target_churn_total: CounterVec`, `pub aws_api_calls_total: CounterVec`, `pub cache_follower_syncs_total: CounterVec`, `pub startup_duration_seconds: Gauge`
    - `src/metrics/mod.rs` contains the exact metric name strings `ecs_sd_http_requests_total`, `ecs_sd_http_request_duration_seconds`, `ecs_sd_discovery_targets_per_cluster`, `ecs_sd_discovery_target_churn_total`, `ecs_sd_aws_api_calls_total`, `ecs_sd_cache_follower_syncs_total`, `ecs_sd_startup_duration_seconds`
    - `rg -c 'registry\.register\(Box::new\(' src/metrics/mod.rs` returns 16
    - `cargo build` exits 0
  </acceptance_criteria>
  <done>All 7 fields exist, are constructed with the exact metric names/labels/buckets above, are registered in the shared `registry`, and the crate compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add registration + label-set tests for the 7 new metric families</name>
  <files>src/metrics/tests.rs</files>
  <read_first>
    - src/metrics/tests.rs (replicate the `MetricsState::new().unwrap()` + `registry.gather()` + `families.iter().any(|f| f.name() == ...)` assertion style)
    - src/metrics/mod.rs (field names and label counts finalized in Task 1)
  </read_first>
  <action>
    Append tests to `src/metrics/tests.rs` (same `use super::*;` module) following the existing gather-based style. Because `CounterVec`/`GaugeVec`/`HistogramVec` families only appear in `registry.gather()` after first `with_label_values`, each test must exercise the vec before gathering:
    - `http_requests_total_countervec_works`: call `.with_label_values(&["/sd", "GET", "200"]).inc()`, gather, assert a family named `ecs_sd_http_requests_total` exists and has 1 metric.
    - `http_request_duration_histogramvec_works`: call `.with_label_values(&["/sd", "GET"]).observe(0.005)`, gather, assert family `ecs_sd_http_request_duration_seconds` exists.
    - `discovery_targets_per_cluster_gaugevec_works`: call `.with_label_values(&["prod"]).set(3.0)`, gather, assert family `ecs_sd_discovery_targets_per_cluster` exists.
    - `discovery_target_churn_countervec_works`: increment both `["added"]` and `["removed"]`, gather, assert family `ecs_sd_discovery_target_churn_total` has 2 metrics.
    - `aws_api_calls_countervec_works`: increment `["list_tasks"]` and `["describe_tasks"]`, gather, assert family `ecs_sd_aws_api_calls_total` has 2 metrics.
    - `cache_follower_syncs_countervec_works`: increment `["success"]`, `["error"]`, `["skipped_leader"]`, gather, assert family `ecs_sd_cache_follower_syncs_total` has 3 metrics.
    - `startup_duration_gauge_works`: `.set(1.5)`, gather, assert family `ecs_sd_startup_duration_seconds` exists.
    Use the prefix-free descriptive `snake_case` naming style (not the older `test_` prefix). Do not modify the existing `metrics_state_has_registry` test.
  </action>
  <verify>
    <automated>cd /Users/piotrek/git/ecs-sd && cargo test metrics 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/metrics/tests.rs` contains test fns `http_requests_total_countervec_works`, `http_request_duration_histogramvec_works`, `discovery_targets_per_cluster_gaugevec_works`, `discovery_target_churn_countervec_works`, `aws_api_calls_countervec_works`, `cache_follower_syncs_countervec_works`, `startup_duration_gauge_works`
    - `cargo test metrics` exits 0 with the 7 new tests passing
    - The churn test asserts exactly 2 metrics; the aws-calls test asserts exactly 2; the follower-syncs test asserts exactly 3
  </acceptance_criteria>
  <done>Seven new tests pass, proving each new family registers, gathers under its exact metric name, and accepts its declared label set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metric registry → Prometheus scrape | Label sets defined here bound the cardinality that a scraper (and this process's memory) will ever hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Denial of Service | metric label sets in MetricsState::new | mitigate | All new labels are finite by design: `status_code` (HTTP status set), `method` (HTTP verb set), `change` (2 values: added/removed), `result` (3 values), `operation` (fixed AWS op names). No user-controlled label values are introduced in this plan. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero new crates this phase — RESEARCH `## Package Legitimacy Audit` records no packages added; existing prometheus 0.14 already pinned in Cargo.lock. |
</threat_model>

<verification>
- `cargo build` compiles with the 7 new fields.
- `cargo test metrics` green (existing + 7 new tests).
- `rg -c 'registry\.register\(Box::new\(' src/metrics/mod.rs` returns 16 (9 existing + 7 new).
</verification>

<success_criteria>
MetricsState exposes 7 new public fields with the exact metric names and label sets specified, all registered in the shared custom Registry, and unit tests prove each family is gatherable with its declared labels.
</success_criteria>

<output>
Create `.planning/phases/12-http-metrics-middleware-new-metric-families/12-01-SUMMARY.md` when done.
</output>
