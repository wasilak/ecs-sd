---
phase: 12-http-metrics-middleware-new-metric-families
plan: 04
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - src/main.rs
  - src/handlers/sd.rs
autonomous: true
requirements: [MET-10, MET-11, MET-13, MET-14]
must_haves:
  truths:
    - "All three cache-replacement paths (initial discovery, background refresh, follower sync) call replace_cache_and_record_metrics so per-cluster + churn metrics update on every refresh"
    - "The manual POST /sd/refresh handler also calls replace_cache_and_record_metrics"
    - "startup_duration_seconds is set exactly once, in the initial-discovery success branch, never on subsequent refreshes"
    - "Follower sync records cache_follower_syncs_total{skipped_leader|success|error}; the None (no leader cache yet) case records nothing"
  artifacts:
    - path: "src/main.rs"
      provides: "MET-14 startup recording + MET-13 follower instrumentation + record-metrics wiring"
      contains: "startup_duration_seconds"
    - path: "src/handlers/sd.rs"
      provides: "manual refresh path uses record-metrics helper"
      contains: "replace_cache_and_record_metrics"
  key_links:
    - from: "src/main.rs initial discovery Ok branch"
      to: "state.metrics.startup_duration_seconds"
      via: "set(started_at.elapsed().as_secs_f64()) once"
      pattern: "startup_duration_seconds\\.set"
    - from: "src/main.rs spawn_follower_sync"
      to: "state.metrics.cache_follower_syncs_total"
      via: "with_label_values inc per outcome"
      pattern: "cache_follower_syncs_total"
---

<objective>
Wire the operational metrics into every path that replaces the cache, so MET-10 (per-cluster counts) and MET-11 (churn) update on all refreshes, MET-13 (follower sync outcomes) is recorded in the follower task, and MET-14 (startup duration) is recorded exactly once at first successful population. This is the final wave: it depends on the `replace_cache_and_record_metrics` method from plan 12-03 and the main.rs changes from plan 12-02.

Purpose: Churn and per-cluster metrics are only meaningful if every cache-replacement path records them (background loop, manual refresh, follower sync). Startup duration must be recorded once — not on every background cycle.
Output: `src/main.rs` initial discovery, `refresh_cache_once`, and `spawn_follower_sync` updated; `src/handlers/sd.rs` `refresh_handler` updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-http-metrics-middleware-new-metric-families/12-RESEARCH.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from source-drift verification):

- No new public symbols. This plan wires existing symbols:
  - Replaces `state.replace_cache_and_routing(...)` with `state.replace_cache_and_record_metrics(...)` (from plan 12-03) in `src/main.rs` (initial discovery, `refresh_cache_once`), `src/handlers/sd.rs` (`refresh_handler`), and `src/main.rs` (`spawn_follower_sync`)
  - New one-shot recording of `state.metrics.startup_duration_seconds` in the initial-discovery success branch (MET-14)
  - New `state.metrics.cache_follower_syncs_total.with_label_values(&[...])` increments in `spawn_follower_sync` (MET-13)
  - Consumes MetricsState fields `startup_duration_seconds`, `cache_follower_syncs_total` (created in plan 12-01) and the `replace_cache_and_record_metrics` method (plan 12-03)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire record-metrics + MET-14 + MET-13 into main.rs refresh paths</name>
  <files>src/main.rs</files>
  <read_first>
    - src/main.rs (initial discovery block lines 127-149 with the `Ok(targets_aws)` branch at 130-138; `refresh_cache_once` lines 302-320; `spawn_follower_sync` lines 381-412)
    - src/state/app_state.rs (confirm `replace_cache_and_record_metrics` signature and that `started_at: std::time::Instant` exists on AppState)
    - .planning/phases/12-http-metrics-middleware-new-metric-families/12-RESEARCH.md (Pattern 5 — MET-14 one-shot; Pattern 6 — follower instrumentation; Pitfall 4 — never set MET-14 in refresh_cache_once; Assumption A3 — None from read_leader_cache is NOT an error)
  </read_first>
  <action>
    Initial discovery (main.rs, `Ok(targets_aws)` branch ~line 130-138): replace `state.replace_cache_and_routing(targets_aws).await;` with `state.replace_cache_and_record_metrics(targets_aws).await;`. Immediately after that call, record MET-14 exactly once: compute `let startup_secs = state.started_at.elapsed().as_secs_f64();`, call `state.metrics.startup_duration_seconds.set(startup_secs);`, and update the existing `info!("Initial discovery complete")` log to include the startup seconds (e.g. include `startup_secs`). Keep the existing `publish_cache_to_gossip(&state).await;` and `last_refresh_outcome` write that follow.

    `refresh_cache_once` (main.rs ~line 317): replace `state.replace_cache_and_routing(targets_aws).await;` with `state.replace_cache_and_record_metrics(targets_aws).await;`. Do NOT set `startup_duration_seconds` here (Pitfall 4 — MET-14 is startup-only).

    `spawn_follower_sync` (main.rs ~lines 389-404): add MET-13 instrumentation without changing the control flow's intent:
    - At the leader check (currently `if cluster.is_leader().await { continue; }`), before `continue`, increment `state.metrics.cache_follower_syncs_total.with_label_values(&["skipped_leader"]).inc();`.
    - In the `Ok(targets)` arm, replace `state.replace_cache_and_routing(targets).await;` with `state.replace_cache_and_record_metrics(targets).await;` and then increment `cache_follower_syncs_total.with_label_values(&["success"]).inc();`.
    - In the `Err(e)` arm (malformed JSON), increment `cache_follower_syncs_total.with_label_values(&["error"]).inc();` alongside the existing `tracing::warn!`.
    - Do NOT add any counter for the `None` return from `read_leader_cache()` (leader has not published yet — a normal startup transient, per Assumption A3). The existing `if let Some(json)` already skips it.
  </action>
  <verify>
    <automated>cd /Users/piotrek/git/ecs-sd && cargo build 2>&1 | tail -6 && echo "startup_set=$(rg -c 'startup_duration_seconds\.set' src/main.rs) record=$(rg -c 'replace_cache_and_record_metrics' src/main.rs) follower=$(rg -c 'cache_follower_syncs_total' src/main.rs)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.rs` contains `state.metrics.startup_duration_seconds.set(` exactly once (`rg -c 'startup_duration_seconds\.set' src/main.rs` returns 1)
    - `src/main.rs` initial-discovery and `refresh_cache_once` both call `replace_cache_and_record_metrics` (`rg -c 'replace_cache_and_record_metrics' src/main.rs` returns 3: initial, refresh_cache_once, follower Ok arm)
    - `src/main.rs` does NOT set `startup_duration_seconds` inside `refresh_cache_once` (the single `.set` occurrence is in the initial-discovery block)
    - `src/main.rs` `spawn_follower_sync` contains `cache_follower_syncs_total.with_label_values(&["skipped_leader"])`, `&["success"]`, and `&["error"]` and no counter for the `None` case
    - `src/main.rs` no longer calls `replace_cache_and_routing` in these three paths (`rg -c 'replace_cache_and_routing' src/main.rs` returns 0)
    - `cargo build` exits 0
  </acceptance_criteria>
  <done>All three main.rs cache-replacement paths use `replace_cache_and_record_metrics`, MET-14 is recorded once at startup, and MET-13 records skipped_leader/success/error (never the None case).</done>
</task>

<task type="auto">
  <name>Task 2: Wire record-metrics into the manual refresh handler (sd.rs)</name>
  <files>src/handlers/sd.rs</files>
  <read_first>
    - src/handlers/sd.rs (`refresh_handler` lines 142-200, specifically the `Ok(targets_aws)` arm at line 178-187 calling `state.replace_cache_and_routing(targets_aws).await;`)
    - src/state/app_state.rs (confirm `replace_cache_and_record_metrics` signature)
  </read_first>
  <action>
    In `refresh_handler` (`src/handlers/sd.rs`, `Ok(targets_aws)` arm ~line 180), replace `state.replace_cache_and_routing(targets_aws).await;` with `state.replace_cache_and_record_metrics(targets_aws).await;`. Preserve the surrounding `let count = targets_aws.len();` (compute before the move), the `info!` log, and the JSON response. This ensures the manual POST /sd/refresh path also updates per-cluster and churn metrics (MET-10, MET-11). Do not change authorization, rate-limiting, or error handling.
  </action>
  <verify>
    <automated>cd /Users/piotrek/git/ecs-sd && cargo test 2>&1 | tail -8 && rg -n 'replace_cache_and_record_metrics|replace_cache_and_routing' src/handlers/sd.rs</automated>
  </verify>
  <acceptance_criteria>
    - `src/handlers/sd.rs` `refresh_handler` calls `state.replace_cache_and_record_metrics(targets_aws)` in the success arm
    - `src/handlers/sd.rs` no longer calls `replace_cache_and_routing` (`rg -c 'replace_cache_and_routing' src/handlers/sd.rs` returns 0)
    - `let count = targets_aws.len();` still precedes the record-metrics call (count computed before the vec is moved)
    - `cargo test` full suite exits 0
  </acceptance_criteria>
  <done>The manual refresh handler records per-cluster and churn metrics via the shared method, and the full suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gossip (follower sync) → cache | Leader-published cache JSON is deserialized in the follower; malformed payloads must be counted as `error`, not crash the task |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Denial of Service | cache_follower_syncs_total result label | mitigate | `result` label is one of 3 hardcoded literals (`skipped_leader`, `success`, `error`); the `None`-no-data case is deliberately uncounted to avoid a misleading error signal (Assumption A3). |
| T-12-08 | Tampering | follower deserialization of gossip JSON | accept | Existing `serde_json::from_str` error handling is preserved; malformed JSON increments `error` and logs a warning without replacing the cache. No new deserialization surface introduced. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero new crates (RESEARCH `## Package Legitimacy Audit`). |
</threat_model>

<verification>
- `cargo build` compiles all four wiring sites.
- `cargo test` full suite green (existing ~170 tests, including main.rs structural/contract tests, unaffected).
- `rg -c 'startup_duration_seconds\.set' src/main.rs` returns 1 (MET-14 recorded once).
- `rg -c 'replace_cache_and_routing' src/main.rs src/handlers/sd.rs` returns 0 (all refresh paths migrated).
</verification>

<success_criteria>
Every cache-replacement path records per-cluster and churn metrics through `replace_cache_and_record_metrics`, startup duration is set exactly once at first successful population, follower sync outcomes are counted with the three result labels (None excluded), and the full test suite passes.
</success_criteria>

<output>
Create `.planning/phases/12-http-metrics-middleware-new-metric-families/12-04-SUMMARY.md` when done.
</output>
