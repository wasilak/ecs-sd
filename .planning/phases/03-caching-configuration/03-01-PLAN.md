---
phase: 03-caching-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/config.rs
  - src/main.rs
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - CONF-06
must_haves:
  truths:
    - "Operator can start ecs-sd with CLI flags only (clusters required, other values defaulted)."
    - "Operator can start ecs-sd with ECS_SD_* environment variables only."
    - "Invalid startup config exits before server start with explicit error."
  artifacts:
    - path: "src/config.rs"
      provides: "clap derive Args + Config conversion + validation"
    - path: "src/main.rs"
      provides: "startup path using parsed config instead of hardcoded values"
    - path: "Cargo.toml"
      provides: "clap derive/env + humantime + rand dependencies"
  key_links:
    - from: "src/config.rs"
      to: "src/main.rs"
      via: "Config::from_process_args() call"
      pattern: "Config::from_process_args"
    - from: "CLI/ECS_SD_*"
      to: "Config"
      via: "clap #[arg(env = ...)] precedence"
      pattern: "env = \"ECS_SD_"
---

<objective>
Implement startup configuration parsing with clap derive macros and strict validation so runtime behavior in later plans consumes a single validated Config.

Purpose: Implement D-02 and D-03 exactly (flat clap flags, env support, precedence CLI > env > defaults, fail-fast validation).
Output: Deterministic runtime config loading for clusters/listen/refresh_interval/metadata_level plus required dependencies.
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-caching-configuration/03-CONTEXT.md
@.planning/phases/03-caching-configuration/03-RESEARCH.md
@.planning/phases/03-caching-configuration/03-PATTERNS.md
@src/config.rs
@src/main.rs
@src/models/metadata_level.rs
@src/aws/client.rs

<interfaces>
From src/config.rs (current runtime contract):
```rust
pub struct Config {
    pub clusters: Vec<String>;
    pub listen: String;
    pub refresh_interval: u64;
    pub metadata_level: MetadataLevel;
}
```

From src/models/metadata_level.rs (must be reused for clap value parsing):
```rust
impl FromStr for MetadataLevel {
    type Err = String;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace placeholder config with clap derive + env-backed parser (D-02, D-03)</name>
  <read_first>
    - src/config.rs
    - src/models/metadata_level.rs
    - .planning/phases/03-caching-configuration/03-CONTEXT.md
    - .planning/phases/03-caching-configuration/03-RESEARCH.md
  </read_first>
  <files>src/config.rs</files>
  <action>Implement `#[derive(Parser, Debug, Clone)] pub struct Args` with exact fields and mappings: `clusters` (`--clusters`, env `ECS_SD_CLUSTERS`, required=true), `listen` (`--listen`, env `ECS_SD_LISTEN`, default `0.0.0.0:8080`), `refresh_interval` (`--refresh-interval`, env `ECS_SD_REFRESH_INTERVAL`, default `60s`, parser `humantime::parse_duration`), `metadata_level` (`--metadata-level`, env `ECS_SD_METADATA_LEVEL`, default `task`, value parser `MetadataLevel::from_str`). Add `Config::from_process_args() -> Result<Config, ConfigError>` and `Config::from_iter<I,T>(iter) -> Result<Config, ConfigError>` for tests. Validation rules must be explicit: parsed clusters vector from comma-separated string must contain at least one non-empty entry; listen must parse as `SocketAddr`; refresh interval must be `> Duration::ZERO`. Keep runtime `Config.refresh_interval` as seconds (`u64`) by converting parsed Duration with `as_secs()` after validation. Add focused unit tests in `src/config.rs` test module for CLI precedence over env, env fallback when CLI absent, default fallbacks, empty clusters rejection, invalid listen rejection, and zero refresh interval rejection.</action>
  <acceptance_criteria>
    - `rg -n "derive\(Parser" src/config.rs` returns one `Args` derive declaration.
    - `rg -n "ECS_SD_CLUSTERS|ECS_SD_LISTEN|ECS_SD_REFRESH_INTERVAL|ECS_SD_METADATA_LEVEL" src/config.rs` returns all four env mappings.
    - `rg -n "default_value = \"0.0.0.0:8080\"|default_value = \"60s\"|default_value = \"task\"" src/config.rs` returns all three defaults.
    - `rg -n "from_process_args|from_iter" src/config.rs` returns both constructors.
    - `cargo test config:: -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test config:: -- --nocapture</automated>
  </verify>
  <done>Config parsing behavior matches D-02/D-03 exactly with deterministic tests for precedence and validation failures.</done>
</task>

<task type="auto">
  <name>Task 2: Wire startup to parsed config and add required dependencies (D-02, D-03, CONF-06 guard)</name>
  <read_first>
    - Cargo.toml
    - src/main.rs
    - src/aws/client.rs
    - src/config.rs
    - .planning/phases/03-caching-configuration/03-CONTEXT.md
  </read_first>
  <files>Cargo.toml, src/main.rs</files>
  <action>Add dependencies in `Cargo.toml`: `clap = { version = "4", features = ["derive", "env"] }`, `humantime = "2"`, `rand = "0.8"`. Replace hardcoded `Config::new(vec![...])` in `src/main.rs` with `Config::from_process_args()` and exit non-zero with explicit startup error on parse failure before creating AWS clients. Keep existing AWS credential loading path unchanged (CONF-06): `aws::client::create_clients()` must continue using aws-config default provider chain from `src/aws/client.rs` without introducing custom credential logic.</action>
  <acceptance_criteria>
    - `rg -n "clap = \{|humantime = \"2\"|rand = \"0.8\"" Cargo.toml` returns all three dependency declarations.
    - `rg -n "Config::from_process_args" src/main.rs` returns exactly one startup call.
    - `rg -n "Config::new\(" src/main.rs` returns no matches.
    - `rg -n "RegionProviderChain::default_provider\(\)" src/aws/client.rs` still returns the default-provider-chain call (CONF-06 preserved).
    - `cargo test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test</automated>
  </verify>
  <done>Binary startup behavior is configuration-driven (CLI/env/default) with fail-fast validation and preserved AWS credential-chain behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input → config parser | untrusted CLI/env values cross into runtime behavior |
| process env/IAM role → AWS SDK credential resolver | external credential source controls cloud API access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `src/config.rs` argument parsing | mitigate | Validate `--listen` as `SocketAddr`, `--clusters` non-empty after split/trim, and `--refresh-interval > 0` before startup; reject invalid input with non-zero exit. |
| T-03-02 | Denial of Service | startup configuration path | mitigate | Fail fast before server bind for invalid values; prevent runtime panic loops from malformed operator input. |
| T-03-03 | Elevation of Privilege | AWS credential resolution path | mitigate | Preserve default aws-config provider chain usage (`RegionProviderChain::default_provider`) and do not add custom credential override logic in this phase (per CONF-06). |
</threat_model>

<verification>
- `cargo test config:: -- --nocapture`
- `cargo test`
- `cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- `ecs-sd --clusters prod` starts with defaults: `listen=0.0.0.0:8080`, `refresh_interval=60s`, `metadata_level=task`.
- `ECS_SD_CLUSTERS=prod ECS_SD_LISTEN=127.0.0.1:18080 ecs-sd` starts without CLI flags.
- CLI value overrides env (`--refresh-interval 30s` beats `ECS_SD_REFRESH_INTERVAL=120s`).
- Invalid config (e.g., `--listen bad`) exits before AWS client creation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-caching-configuration/03-01-SUMMARY.md`
</output>
