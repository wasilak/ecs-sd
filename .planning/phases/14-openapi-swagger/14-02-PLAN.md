---
phase: 14-openapi-swagger
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/openapi.rs
autonomous: true
requirements: [API-03, API-04]

must_haves:
  truths:
    - "A unit test validates the OpenAPI spec is parseable JSON with correct version, all 8 paths, and all 10 schemas"
    - "cargo test passes with the new test included"
  artifacts:
    - path: "src/openapi.rs"
      provides: "openapi_spec_is_valid test function verifying spec structure"
      contains: "#[test]"
  key_links:
    - from: "src/openapi.rs"
      to: "utoipa::OpenApi"
      via: "ApiDoc::openapi().to_json() spec generation"
      pattern: "ApiDoc::openapi"
---

<objective>
Add compile-time and unit test verification that the OpenAPI spec is structurally valid.

Purpose: Prevent regressions where adding/removing endpoints or changing schemas breaks the spec. The test validates that ApiDoc::openapi() produces a valid OpenAPI 3.0 document with all expected paths and schemas.

Output: New test in src/openapi.rs that passes via `cargo test`.
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-openapi-swagger/14-RESEARCH.md
@.planning/phases/14-openapi-swagger/14-PATTERNS.md
@.planning/phases/14-openapi-swagger/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OpenAPI spec validation unit test in src/openapi.rs</name>
  <files>src/openapi.rs</files>

  <read_first>
    - src/openapi.rs (the file created in Plan 01 — must read current state before editing)
    - .planning/phases/14-openapi-swagger/14-RESEARCH.md (validation architecture section — test map for API-03, API-04)
  </read_first>

  <action>
    Add a `#[cfg(test)] mod tests` block at the bottom of src/openapi.rs with the following test:

    `openapi_spec_is_valid`:
    - Call `crate::openapi::ApiDoc::openapi()` to generate the spec
    - Call `.to_json()` on the spec to get the JSON string
    - Parse it as `serde_json::Value`
    - Assert `body["openapi"] == "3.0.3"` (utoipa generates OpenAPI 3.0.3)
    - Assert `body["info"]["title"] == "ecs-sd"`
    - Assert `body["paths"]` contains keys: `"/sd"`, `"/sd/refresh"`, `"/health"`, `"/health/live"`, `"/health/ready"`, `"/metrics"`, `"/config"`, `"/proxy/{id}/{path}"`
    - Assert `body["components"]["schemas"]` contains keys: `"Target"`, `"MetadataLevel"`, `"FilterMode"`, `"HealthResponse"`, `"CacheHealth"`, `"ClusterHealth"`, `"LastRefreshHealth"`, `"ConfigResponse"`, `"Mode"`, `"ClusterMode"`

    The test file will need `use utoipa::OpenApi;` at the top of the test module (or it may already be in scope from the parent module).

    After adding the test, run `cargo test openapi_spec_is_valid` to verify it passes. Then run the full `cargo test` suite to ensure nothing is broken.
  </action>

  <verify>
    <automated>cargo test openapi_spec_is_valid -- --nocapture 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - src/openapi.rs contains `#[cfg(test)] mod tests` block
    - Test module contains `fn openapi_spec_is_valid()` annotated with `#[test]`
    - Test asserts `openapi` version is `"3.0.3"`
    - Test asserts all 8 endpoint paths exist in `body["paths"]`
    - Test asserts all 10 schema names exist in `body["components"]["schemas"]`
    - `cargo test openapi_spec_is_valid` exits 0
    - `cargo test` exits 0 (all 201 existing tests + 1 new test pass)
  </acceptance_criteria>

  <done>Unit test validates OpenAPI 3.0 spec structure: correct version, all 8 paths present, all 10 schemas present. All tests pass.</done>
</task>

</tasks>

<verification>
1. `cargo test` exits 0 — all 201 existing tests + 1 new OpenAPI validation test pass
2. The new test catches regressions: removing an endpoint from a handler annotation or forgetting a ToSchema derive will cause the test to fail
</verification>

<success_criteria>
- `openapi_spec_is_valid` test exists and passes
- Test validates OpenAPI version, all 8 paths, all 10 schemas
- Full test suite remains green
</success_criteria>

<output>
Create `.planning/phases/14-openapi-swagger/14-02-SUMMARY.md` when done
</output>
